meta {
  name: Delete Product
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/products/1
  body: none
  auth: none
}

headers {
  Cookie: token={{token}}
  X-Organization-ID: {{orgId}}
}

docs {
  # Delete Product
  
  Deletes product (soft delete - is_active = false).
}
