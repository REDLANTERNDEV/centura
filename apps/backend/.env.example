# Server Configuration
PORT=8765
NODE_ENV=development

# Frontend Configuration (for CORS)
# Development: http://localhost:4321
# Production: https://app.yourdomain.com
FRONTEND_URL=http://localhost:4321

# CORS Origins - Comma separated, NO SPACES!
# Should match FRONTEND_URL in most cases
# Can include multiple origins: http://localhost:4321,https://app.yourdomain.com
CORS_ORIGIN=http://localhost:4321

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mini_saas_erp
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# JWT Configuration
JWT_SECRET=your-super-secure-jwt-secret-key-here-at-least-256-bits
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Session Configuration (for CSRF protection)
SESSION_SECRET=your-session-secret-key-here-different-from-jwt

# Cookie Configuration
# Development: leave COOKIE_DOMAIN empty (undefined)
# Production with specific subdomain: app.yourdomain.com
# Production sharing cookies across subdomains: .yourdomain.com
COOKIE_DOMAIN=